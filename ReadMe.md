Softwares required
	1. Eclipse for JEE Developers
	2. Hibernate 5.x http://hibernate.org/orm/releases/
	3  Spring Framework 
	4  Database (MySQL/Oracle or any DB)


To run the code add the dependent jar files and configure the Database connection in the hibernate.cfg.xml or the applicationContext.xml
